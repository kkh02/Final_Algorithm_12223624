# 모티베이션 & 인트로

본 프로젝트의 목표는 18개 앵커에서 측정된 거리값을 이용하여 사용자의 2차원 위치를 추정하는 것이다. 중간발표까지의 실험과 데이터 분석을 통해, 단순 삼각측량이나 일반 Least Squares 방식만으로는 안정적인 위치추정이 어렵다는 점을 확인하였다. 거리 측정값에는 앵커별 bias, 이상치, 다중경로로 인한 오차, 특정 앵커의 순간적인 불안정성이 함께 포함되어 있었다. 따라서 모든 앵커를 동일하게 신뢰하여 위치를 계산하면, 일부 앵커의 오차가 최종 위치를 크게 흔드는 문제가 발생할 수 있다고 판단하였다.

초기에는 여러 앵커 조합으로 위치 후보를 만들고, DBSCAN과 같은 군집화 기법으로 가장 일관된 후보군을 선택하는 방식을 고려하였다. 이 방식은 여러 후보 위치 중 밀집된 영역을 선택할 수 있다는 장점이 있다. 하지만 hidden test 데이터에서는 거리 오차의 분포가 달라질 수 있고, 이 경우 군집 반경이나 후보점 분포에 따라 잘못된 군집이 선택될 위험이 있다고 보았다. 즉, 학습 데이터에서 잘 동작하는 군집 파라미터가 hidden data에서도 안정적으로 동작한다고 보장하기 어렵다고 판단하였다. 그래서 최종 알고리즘에서는 후보 위치를 군집화하는 방식보다, 각 앵커가 최종 위치해에 미치는 영향을 직접 평가하는 방향으로 수정하였고, 이 변화가 본 알고리즘의 핵심 차별점이다. 이를 위해 Leave-One-Out 자기진단 방식을 도입하였다. 이 방식은 앵커를 하나씩 제거하면서 위치를 다시 계산하고, 특정 앵커를 제거했을 때 위치 결과가 얼마나 변하는지를 측정하는 방식이다. 해당 앵커를 제거했을 때 위치 결과가 크게 변한다면, 그 앵커는 전체 위치추정 결과를 강하게 흔드는 앵커일 가능성이 있다.

최종적으로 제안한 알고리즘은 LOO-TWAMCC이다. 이는 Leave-One-Out Self-Diagnosed Trust-Weighted Adaptive MCC의 약자이다. 본 알고리즘은 앵커별 거리 보정, 학습 데이터 기반 history 신뢰도, Leave-One-Out 자기진단 신뢰도, MAD 기반 residual score, Adaptive MCC weight를 결합하여 최종 위치를 추정한다. 핵심 목표는 단순히 residual이 큰 앵커를 제거하는 것이 아니라, 각 앵커가 전체 위치추정 구조에 미치는 영향을 반영하여 더 안정적인 위치해를 얻는 것이다.

# 알고리즘 설명

본 알고리즘은 정답 위치를 사용할 수 있는 학습 단계와 정답 위치를 사용할 수 없는 추론 단계로 나누어 구성하였다. 

학습 단계에서는 제공된 DH_FR1.mat 파일에서 앵커 좌표, 거리 측정값, 정답 위치를 읽는다. 각 사용자 위치와 각 앵커 좌표 사이의 실제 거리를 계산한 뒤, 앵커별로 측정 거리와 실제 거리 사이의 관계를 학습한다. 이때 모든 앵커에 하나의 공통 보정식을 적용하지 않고, 앵커마다 서로 다른 선형 보정식을 사용하였다. 앵커마다 설치 위치와 주변 환경이 다를 수 있고, 거리 오차의 경향도 다를 수 있기 때문이다. 
앵커 i에 대한 보정 거리는 d_corr_i = a_i * d_hat_i + b_i로 계산하였다. 여기서 d_hat_i는 앵커 i에서 측정된 거리이고, d_corr_i는 보정된 거리이다. a_i와 b_i는 학습 데이터에서 구한 앵커별 보정 계수이다. 보정 계수를 학습할 때는 일반적인 최소제곱 회귀만 사용하지 않고 robust loss를 적용하였다. 거리 측정값에는 이상치가 포함될 수 있고 이로 인해 단순 최소제곱 방식은 일부 큰 오차값에 의해 보정식이 왜곡될 수 있기 때문이다. 따라서 robust fitting을 적용함으로써 큰 오차값의 영향을 줄이고, 앵커별 거리 보정 계수를 더 안정적으로 학습하였다.
보정 후에는 앵커별 residual을 계산하였다. 보정 후에도 residual이 작은 앵커는 학습 데이터에서 안정적으로 동작한 앵커라고 판단하고 높은 history 신뢰도를 부여하였다. 반대로 보정 후 residual이 큰 앵커는 기본 신뢰도를 낮게 설정하였다. 최종적으로 train.py는 앵커별 보정 계수, history 신뢰도, residual 통계량, Adaptive MCC에 필요한 scale 파라미터를 model.npz로 저장한다.

추론 단계에서는 먼저 DH_FR1.mat에서 앵커 좌표와 거리 측정값을 읽고, model.npz에서 앵커별 보정 계수와 history 신뢰도를 불러온다. 입력 거리값은 학습된 보정식을 이용해 앵커별로 보정된다. 이후 보정된 거리값을 이용하여 Calibrated Least Squares 기반의 기준 위치 x_base를 계산한다. 이 위치는 최종 위치가 아니라, 앵커별 영향도를 진단하기 위한 기준 위치이다. 다음으로 Leave-One-Out 자기진단을 수행한다. 전체 앵커를 사용해 얻은 기준 위치를 x_base라고 하고, 앵커 i를 제거한 상태에서 다시 계산한 위치를 x_minus_i라고 한다. 
앵커 i의 영향도는 두 위치 사이의 거리를 사용하여 I_i = distance(x_base, x_minus_i)로 구한다. 이때 I_i가 크다는 것은 해당 앵커를 제거했을 때 위치 결과가 크게 바뀐다는 뜻이다. 즉, 그 앵커가 전체 위치추정 결과에 큰 영향을 주고 있을 가능성이 있다. 반대로 I_i가 작다면 해당 앵커를 제거해도 위치 결과가 크게 변하지 않으므로 비교적 안정적인 앵커라고 볼 수 있다. 하지만 I_i가 크다고 해서 해당 앵커를 무조건 나쁜 앵커로 판단하지는 않았다. 어떤 앵커는 기하학적으로 중요한 위치에 있기 때문에 제거했을 때 위치가 크게 변할 수도 있기 때문이다. 따라서 본 알고리즘에서는 Leave-One-Out 영향도만 단독으로 사용하지 않고, 기준 위치에서의 residual 이상 정도와 함께 결합하였다. 즉, 위치 변화량도 크고 residual도 전체 residual 분포에서 벗어난 앵커일수록 불안정한 앵커로 판단한 것이다.

residual의 이상 정도는 MAD를 이용하여 계산하였다. MAD는 중앙값 기반 통계량이므로 평균과 표준편차보다 이상치에 덜 민감해서 거리 측정값에 큰 오차가 섞여 있는 상황에서도 residual 분포의 scale을 비교적 안정적으로 추정할 수 있다. 이 특성을 이용해 각 앵커의 residual이 전체 residual 분포에서 얼마나 벗어났는지를 MAD 기반 score로 반영하였다. 또한 Adaptive MCC weight를 적용하였다. MCC는 residual이 작은 데이터에는 큰 가중치를 주고, residual이 큰 데이터에는 작은 가중치를 주는 robust weight 방식이다. 본 알고리즘에서는 MCC의 scale 값을 고정된 threshold로 두지 않고, residual 분포의 MAD를 이용해 자동으로 조절하였다. 이를 통해 데이터마다 residual 크기가 달라져도 특정 고정 임계값에 과도하게 의존하지 않도록 하였다.

LOO 자기진단 신뢰도는 S_LOO_i = 1 / (1 + 0.90 * instability_i) 형태로 계산하였다. 여기서 instability_i는 앵커 i의 Leave-One-Out 영향도와 residual 이상 정도를 함께 반영한 값이다. 0.90은 LOO 영향도를 최종 신뢰도에 반영하는 강도이며, validation split을 통해 선택하였다. 이 값을 너무 크게 설정하면 특정 앵커를 과도하게 낮게 평가할 수 있고, 너무 작게 설정하면 불안정 앵커를 충분히 억제하지 못할 수 있다.

최종 앵커 가중치 W_i는 네 가지 신뢰도 항을 곱한 W_i = S_history_i * S_LOO_i * S_MAD_i * w_MCC_i를 통해 계산하였다. 여기서 S_history_i는 학습 데이터에서 앵커 i가 얼마나 안정적이었는지를 나타내고, S_LOO_i는 앵커 i를 제거했을 때 위치 결과가 얼마나 변하는지를 이용한 Leave-One-Out 자기진단 신뢰도이다. S_MAD_i는 현재 residual 분포에서 앵커 i가 이상치에 가까운지를 나타내는 점수이고, w_MCC_i는 Adaptive MCC 기반 residual weight이다.

최종 위치는 각 앵커에 대해 예측 거리와 보정 거리의 차이가 작아지도록 계산하였다. 이때 모든 앵커를 동일하게 반영하지 않고, 위에서 계산한 W_i를 적용한 weighted nonlinear least squares를 사용하였다. 따라서 신뢰도가 높은 앵커는 위치 계산에 크게 반영되고, 불안정하다고 판단된 앵커는 영향이 줄어든다. 최종 위치를 한 번 계산한 뒤에는 새 위치를 기준으로 MAD와 MCC weight를 다시 계산하여 2차 최적화를 수행하였다. 이를 통해 초기 기준 위치에서 발생할 수 있는 residual 평가 오차를 줄이고, 최종 위치를 더 안정적으로 보정하였다.

main.py의 main 함수는 최종적으로 numpy 배열 형태의 p_hat을 반환한다. 반환 배열의 모양은 (2, num_user)이며, 첫 번째 행은 x 좌표, 두 번째 행은 y 좌표이다. hidden test에서는 사용자 수가 공개 데이터와 다를 수 있으므로, 사용자 수를 코드에 직접 고정하지 않고, d_hat.shape[1]을 이용해 자동으로 계산하도록 구현하였다.

# Agent AI(e.g., ChatGPT, Claude Code, Gemini 등) 활용 방안

본 프로젝트에서는 ChatGPT를 알고리즘 설계, 구현 보조, 결과 해석, 보고서 정리에 활용하였다. 다만 AI가 제안한 내용을 그대로 사용하는 방식이 아니라, 본인이 직접 코드를 실행하고 성능을 비교하면서 최종 알고리즘을 선택하였다.
알고리즘 설계 단계에서는 중간발표까지 확인한 문제점인 앵커별 bias, 이상치, 불안정 앵커의 영향을 줄이기 위한 방법을 ChatGPT와 함께 비교하였다. 초기에는 DBSCAN 기반 후보 군집화, residual 기반 MAD filtering, MCC 기반 robust weighting 등을 후보로 검토하였다. 이후 hidden test에서의 안정성과 알고리즘의 차별성을 고려하여, 최종적으로 Leave-One-Out 자기진단 가중치를 핵심 아이디어로 선택하였다. 이 과정에서 ChatGPT는 가능한 알고리즘 후보와 장단점을 정리하는 역할을 하였고, 본인은 추천받은 알고리즘이 문제 상황에 적합한지, 실제로 구현 가능한지, 실행 결과에서 성능 개선이 나타나는지를 기준으로 최종 방향을 결정하였다.
구현 단계에서는 ChatGPT를 이용해 train.py, main.py, tune.py의 코드 구조를 설계하였다. train.py에서는 학습 데이터의 정답 위치를 이용해 앵커별 거리 보정 계수와 history 신뢰도를 저장하도록 구성하였다. main.py에서는 hidden test의 정답 위치를 사용하지 않고, model.npz와 입력 거리값만으로 위치를 추정하도록 구현하였다. tune.py는 전체 700개 데이터를 일부 train/validation 형태로 나누어 LOO 관련 파라미터를 검증하기 위해 작성하였다.
성능 검증 단계에서는 본인이 직접 코드를 실행하여 Raw Least Squares, Calibrated Least Squares, LOO-TWAMCC의 결과를 비교하였다. ChatGPT는 결과 표 정리와 해석을 보조하였지만, 최종 판단은 직접 실행한 성능 수치와 validation split 결과를 바탕으로 진행하였다. 특히 전체 700개 데이터에만 맞춘 튜닝이 hidden test에 과적합될 수 있다고 판단하여, 여러 random seed를 사용한 validation split 방식으로 파라미터를 선택하였다.
보고서 작성 단계에서는 ChatGPT를 문장 정리와 구조화에 활용하였다. 본인은 실제 구현 흐름, 실험 결과, 알고리즘 선택 이유를 제공하였고, ChatGPT는 이를 최종 보고서 형식에 맞게 정리하는 데 도움을 주었다. 따라서 AI는 코드와 보고서 작성의 보조 도구로 사용되었으며, 문제 정의, 실행 결과 확인, 최종 알고리즘 선택은 본인이 직접 수행하였다.

# 결과 도출 & 디스커션

최종 알고리즘의 성능을 확인하기 위해 Raw Least Squares, Calibrated Least Squares, LOO-TWAMCC를 비교하였다. Raw Least Squares는 거리 보정 없이 측정 거리값을 그대로 사용하는 기본 방식이고, Calibrated Least Squares는 train.py에서 학습한 앵커별 거리 보정만 적용한 방식이다. LOO-TWAMCC는 Calibrated Least Squares에 Leave-One-Out 자기진단, MAD 기반 residual score, Adaptive MCC weight를 추가한 최종 알고리즘이다.

| Method                   |     MAE |    RMSE |  Median |     P90 |     Max |
| ------------------------ | ------: | ------: | ------: | ------: | ------: |
| Raw Least Squares        | 23.1074 | 24.8694 | 21.6895 | 33.4384 | 80.7036 |
| Calibrated Least Squares | 11.3935 | 14.1249 |  9.4372 | 21.2485 | 69.4275 |
| LOO-TWAMCC               |  9.7898 | 12.2692 |  7.8155 | 18.4957 | 56.6512 |

Raw Least Squares는 전체적으로 오차가 크게 나타났다. 이는 측정 거리값에 포함된 앵커별 bias와 이상치가 그대로 위치추정에 반영되었기 때문으로 볼 수 있다. Calibrated Least Squares는 Raw Least Squares보다 성능이 크게 개선되었다. 이를 통해 앵커별 거리 보정이 위치추정 성능 향상에 중요한 역할을 한다는 것을 확인할 수 있었다. 하지만 본 알고리즘의 실제 기여를 판단하기 위해서는 Raw Least Squares보다 Calibrated Least Squares와 LOO-TWAMCC를 비교하는 것이 더 적합하다고 판단하였는데, LOO-TWAMCC도 동일한 앵커별 보정 계수를 사용하기 때문에 단순히 거리 보정이 만든 성능 향상을 제안 알고리즘의 기여로 보기는 어려웠다. 따라서 본 알고리즘의 핵심 기여는 Calibrated Least Squares 이후에 추가된 Leave-One-Out 자기진단, MAD score, Adaptive MCC weight가 얼마나 성능을 향상시켰는지로 판단하는 것이 타당하다고 보았다. 이러한 기준에서 Calibrated Least Squares와 비교했을 때, LOO-TWAMCC는 모든 지표에서 성능이 개선되었다.

| Comparison    |     MAE |    RMSE | Median |     P90 |     Max |
| ------------- | ------: | ------: | -----: | ------: | ------: |
| Calibrated LS | 11.3935 | 14.1249 | 9.4372 | 21.2485 | 69.4275 |
| LOO-TWAMCC    |  9.7898 | 12.2692 | 7.8155 | 18.4957 | 56.6512 |
| Improvement   |   14.1% |   13.1% |  17.2% |   13.0% |   18.4% |

위 결과에서 MAE, RMSE, Median error가 모두 감소한 것은 전체적인 평균 성능뿐만 아니라 일반적인 샘플에 대한 위치추정 성능도 개선되었음을 의미한다. 또한 P90 error와 Max error가 함께 감소한 것은 일부 불안정 앵커로 인해 발생하는 큰 오차를 줄이는 데 LOO 자기진단 가중치가 효과적이었다는 것을 보여준다.

파라미터 선택 과정에서도 hidden test에 대한 일반화 가능성을 고려하였다. 전체 700개 데이터에서 가장 좋은 값만 선택하면 공개 데이터에 과적합될 수 있다. 따라서 별도의 tune.py를 작성하여 validation split 기반 검증을 수행하였다. 전체 데이터 일부를 validation set처럼 분리하고, 나머지 데이터로 보정 계수와 history 신뢰도를 다시 학습한 뒤 validation data에서 성능을 확인하였다. 이 과정을 여러 random seed에서 반복하여 특정 split에만 우연히 잘 맞는 파라미터를 피하고자 하였다. 이때 검증한 주요 파라미터는 residual_base, residual_div, loo_gain이었다. 이들은 LOO 영향도와 residual 이상 정도를 최종 신뢰도에 얼마나 강하게 반영할지를 결정한다. validation split 결과를 바탕으로 최종 설정은 다음과 같이 선택하였다.

| Parameter     | Selected value |
| ------------- | -------------: |
| residual_base |           0.32 |
| residual_div  |            3.2 |
| loo_gain      |           0.90 |

이 자체 평가 방식은 hidden test 상황을 완전히 재현할 수는 없지만, 전체 700개 데이터를 한 번에 모두 사용해 파라미터를 고르는 것보다는 더 공정하다고 판단하였다. validation으로 분리한 데이터는 파라미터 학습에 사용하지 않았기 때문에, 알고리즘이 보지 않은 데이터에서 어느 정도 안정적으로 동작하는지 확인할 수 있었다. 또한 하나의 split에만 의존하지 않고 여러 random seed를 사용하여 결과가 특정 데이터 분할에만 우연히 맞는 문제를 줄이고자 하였다. 이 설정은 LOO 영향도를 너무 강하게 적용하지 않고, 불안정 앵커의 영향을 완만하게 줄이는 방향이다. 실제 validation 결과에서도 LOO 영향도를 과도하게 반영하는 것보다 완만하게 반영하는 설정이 더 안정적으로 나타났다. 따라서 최종 main.py에는 이 파라미터를 적용하였다.

본인의 구현 방향은 문제 상황에 적합하다고 판단하였다. 단순히 복잡한 모델을 추가하는 것보다, 실제 데이터에서 확인된 문제인 앵커별 bias와 불안정 앵커의 영향을 줄이는 데 집중했기 때문이다. train.py에서는 앵커별 거리 보정을 통해 고정적인 bias를 줄였고, main.py에서는 Leave-One-Out 자기진단과 MAD, MCC weight를 사용하여 입력마다 달라지는 불안정 앵커의 영향을 줄이도록 하였다. 따라서 학습 단계와 추론 단계의 역할이 명확하게 분리되어 있으며, hidden test에서 사용할 수 없는 정답 위치를 main.py에서 사용하지 않도록 구현한 점도 적합하다고 판단하였다.

본 알고리즘은 단순히 residual이 큰 앵커를 낮게 평가하는 방식에서 한 단계 더 나아가, 앵커가 전체 위치추정 구조에 미치는 영향을 직접 반영한다는 장점이 있다. 일반적인 residual 기반 방식은 현재 위치에서 각 앵커의 거리 오차가 큰지만 확인하지만, LOO-TWAMCC는 특정 앵커를 제거했을 때 위치 결과가 얼마나 변하는지를 함께 확인한다. 따라서 어떤 앵커가 단순히 오차가 큰 앵커인지, 아니면 실제로 전체 위치 결과를 불안정하게 만드는 앵커인지 더 구체적으로 판단할 수 있다.
또한 train.py에서 앵커별 거리 보정 계수와 history 신뢰도를 학습하기 때문에, 모든 앵커를 동일하게 취급하지 않고 앵커별 오차 특성을 반영할 수 있다. 실제 거리 측정 환경에서는 앵커마다 설치 위치, 주변 구조물, 신호 반사 환경이 다르기 때문에 오차 특성도 다르게 나타날 수 있는데, 본 알고리즘은 이러한 차이를 학습 단계에서 보정하고, 추론 단계에서는 LOO 자기진단과 residual 기반 가중치를 사용하여 입력마다 달라지는 불안정 앵커의 영향을 줄이도록 하였다.
마지막으로 MAD와 Adaptive MCC를 함께 사용하여 이상치에 대해 비교적 부드럽게 대응할 수 있었다. 단순 threshold 방식처럼 특정 기준을 넘는 앵커를 바로 제거하는 것이 아니라, residual 분포를 기준으로 앵커의 신뢰도를 연속적으로 조절하였다. 이 방식은 일부 앵커의 측정값이 불안정하더라도 전체 위치추정 과정이 급격하게 흔들리지 않도록 하는 데 도움이 되었다.
반면 한계도 있다. Leave-One-Out 방식은 각 사용자마다 앵커를 하나씩 제거하면서 위치를 다시 계산하므로 단순 Least Squares보다 계산량이 많다. 현재 데이터에서는 앵커 수가 18개로 제한되어 있어 실행 시간 제한 안에서 동작할 수 있었지만, 앵커 수가 훨씬 많아지는 환경에서는 계산량 최적화가 필요할 수 있다. 또한 기준 위치 x_base가 크게 잘못 계산되는 경우, LOO 영향도 평가도 영향을 받을 수 있다.

향후 개선 방향으로는 LOO 계산을 더 효율적으로 근사하는 방법을 고려할 수 있다. 예를 들어 모든 앵커를 제거해 다시 최적화하는 대신, 선형화된 영향도 근사식을 사용하면 계산량을 줄일 수 있다. 또한 앵커의 기하학적 배치까지 함께 고려하면, 중요한 앵커와 불안정한 앵커를 더 정교하게 구분할 수 있을 것이다.

결론적으로 LOO-TWAMCC는 단순 거리 보정 방식보다 평균 오차와 큰 오차를 모두 줄였다. 또한 baseline과의 비교도 Calibrated Least Squares를 기준으로 수행하여 비교의 공정성을 확보하고자 하였다. 따라서 본 알고리즘은 주어진 데이터셋에서 성능, 안정성, 해석 가능성 측면에서 적합한 최종 위치추정 알고리즘이라고 판단하였다.