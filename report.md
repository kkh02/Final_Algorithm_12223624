# 모티베이션 & 인트로

본 프로젝트의 목표는 18개 앵커에서 측정된 거리값을 이용하여 사용자의 2차원 위치를 추정하는 것이다. 중간발표 단계에서도 앵커별 거리 보정, DBSCAN 기반 후보 군집화, MAD 기반 이상치 판단, MCC 기반 robust weighting과 같은 방법들을 고려하였다. 하지만 당시에는 각 기법을 왜 사용하는지, 그리고 그 기법이 현재 데이터의 어떤 문제를 해결하기에 적합한지에 대한 설명이 부족하였다. 즉, 여러 robust 기법을 조합하는 방향은 생각했지만, 데이터의 오차 구조를 충분히 분석한 뒤 그에 맞게 알고리즘을 설계했다고 보기에는 부족한 부분이 있었다.

중간발표 이후 다시 데이터를 검토하면서, 본 문제에서 중요한 것은 단순히 복잡한 알고리즘을 사용하는 것이 아니라 거리 측정값의 오차가 어떤 방식으로 위치추정 결과를 흔드는지 이해하는 것이라고 판단하였다. 실제 거리 측정값에는 앵커별 bias, 이상치, 다중경로로 인한 오차, 특정 앵커의 순간적인 불안정성이 함께 포함되어 있었다. 따라서 모든 앵커를 동일하게 신뢰하여 위치를 계산하면, 일부 앵커의 잘못된 거리값이 최종 위치를 크게 흔들 수 있었다. 이 고찰을 통해 단순 Least Squares만으로는 안정적인 위치추정이 어렵고, 앵커별 고정 오차와 입력마다 달라지는 불안정 앵커의 영향을 함께 줄여야 한다고 판단하였다.

먼저 앵커별 거리 보정은 각 앵커가 가지는 고정적인 bias를 줄이기 위해 필요하다고 보았다. 모든 앵커에 하나의 공통 보정식을 적용하면 앵커마다 다른 오차 특성을 충분히 반영하기 어렵다. 실제 환경에서는 앵커의 위치, 주변 구조물, 신호 반사 환경이 다르기 때문에 어떤 앵커는 거리값이 전반적으로 크게 측정될 수 있고, 어떤 앵커는 상대적으로 안정적인 값을 줄 수 있다. 따라서 앵커별로 보정 계수를 따로 학습하여, 각 앵커의 측정 거리와 실제 거리 사이의 관계를 보정하는 방향이 적합하다고 판단하였다.

그러나 거리 보정만으로는 충분하지 않았다. 앵커별 bias는 줄일 수 있지만, 특정 입력에서 순간적으로 불안정해지는 앵커의 영향까지 완전히 줄이기는 어렵기 때문이다. 그래서 중간발표 단계에서는 여러 앵커 조합으로 위치 후보를 만들고, DBSCAN과 같은 군집화 기법으로 가장 일관된 후보군을 선택하는 방식을 고려하였다. 이 방식은 여러 후보 위치 중 밀집된 영역을 선택할 수 있다는 장점이 있으며, 이상치 앵커로 인해 생긴 비정상적인 위치 후보를 어느 정도 배제할 수 있다고 생각하였다.

하지만 이후 다시 검토해 보니, DBSCAN 기반 후보 군집화는 hidden test 데이터에서 안정성이 떨어질 수 있다고 판단하였다. DBSCAN은 군집 반경이나 최소 샘플 수 같은 파라미터의 영향을 받는다. 학습 데이터에서는 적절한 군집이 형성되더라도, hidden data에서 거리 오차의 분포가 달라지면 후보점의 밀집 형태가 달라질 수 있고, 이 경우 잘못된 군집이 선택될 위험이 있다. 또한 DBSCAN은 후보 위치들이 서로 모여 있는지는 알려주지만, 특정 앵커가 최종 위치 결과를 얼마나 흔드는지는 직접적으로 알려주지 못한다. 즉, 군집화는 후보 위치의 분포를 보는 방법이지, 앵커 자체의 영향도를 진단하는 방법은 아니라고 판단하였다.

MAD와 MCC는 여전히 본 문제에 적합하다고 보았다. MAD는 평균과 표준편차보다 이상치에 덜 민감하기 때문에 residual 분포에서 비정상적으로 벗어난 앵커를 판단하는 데 적합하다. MCC는 residual이 큰 앵커의 영향을 부드럽게 줄일 수 있기 때문에, 특정 threshold를 기준으로 앵커를 완전히 제거하는 방식보다 안정적이다. 다만 중간발표 때는 MAD와 MCC를 단순히 robust 기법으로만 설명한 측면이 있었다. 이후에는 이 두 방법을 “불안정 앵커를 완전히 제거하는 도구”가 아니라, residual 분포를 기준으로 앵커 신뢰도를 연속적으로 조절하는 도구로 사용하는 것이 더 적합하다고 정리하였다.

최종 알고리즘에서는 기존에 고려했던 앵커별 보정, MAD, MCC는 유지하되, DBSCAN 기반 후보 군집화 대신 Leave-One-Out 자기진단 방식을 핵심 아이디어로 추가하였다. 이 방식은 앵커를 하나씩 제거하면서 위치를 다시 계산하고, 특정 앵커를 제거했을 때 위치 결과가 얼마나 변하는지를 측정한다. 해당 앵커를 제거했을 때 위치 결과가 크게 변한다면, 그 앵커는 전체 위치추정 결과를 강하게 흔드는 앵커일 가능성이 있다. 다만 영향도가 크다고 해서 무조건 나쁜 앵커로 판단하지 않고, residual 이상 정도와 함께 판단하여 기하학적으로 중요한 앵커를 과도하게 낮게 평가하지 않도록 하였다.

최종적으로 제안한 알고리즘은 LOO-TWAMCC이다. 이는 Leave-One-Out Self-Diagnosed Trust-Weighted Adaptive MCC의 약자이다. 본 알고리즘은 앵커별 거리 보정, 학습 데이터 기반 history 신뢰도, Leave-One-Out 자기진단 신뢰도, MAD 기반 residual score, Adaptive MCC weight를 결합하여 최종 위치를 추정한다. 핵심 목표는 단순히 여러 robust 기법을 조합하는 것이 아니라, 앵커별 고정 bias는 학습 단계에서 줄이고, 입력마다 달라지는 불안정 앵커의 영향은 Leave-One-Out, MAD, MCC 기반 신뢰도 가중치로 줄이는 것이다. 이를 통해 각 앵커가 전체 위치추정 구조에 미치는 영향과 residual 이상 정도를 함께 반영하여 더 안정적인 위치해를 얻고자 하였다.

# 알고리즘 설명

본 알고리즘은 정답 위치를 사용할 수 있는 학습 단계와 정답 위치를 사용할 수 없는 추론 단계로 나누어 구성하였다. 학습 단계에서는 train.py를 사용하여 앵커별 거리 보정 계수와 앵커별 기본 신뢰도를 학습하고, 추론 단계에서는 main.py를 사용하여 hidden test의 정답 위치 없이 최종 위치를 추정한다. 이처럼 두 단계를 나눈 이유는 학습 데이터에서는 정답 위치를 사용할 수 있지만, 실제 채점 단계에서는 hidden test의 정답 위치를 사용할 수 없기 때문이다. 따라서 main.py에서는 입력 거리값, 앵커 좌표, 그리고 train.py에서 저장한 model.npz만 사용하도록 구성하였다.

학습 단계에서는 DH_FR1.mat 파일에서 앵커 좌표, 거리 측정값, 정답 위치를 읽는다. 각 사용자 위치와 각 앵커 좌표 사이의 실제 거리를 계산한 뒤, 앵커별로 측정 거리와 실제 거리 사이의 관계를 학습한다. 모든 앵커에 하나의 공통 보정식을 적용하지 않고, 앵커마다 서로 다른 선형 보정식을 사용하였다. 이는 앵커마다 설치 위치, 주변 구조물, 신호 반사 환경이 다를 수 있고, 그에 따라 거리 오차의 방향과 크기도 다르게 나타날 수 있기 때문이다.

앵커 $i$에 대한 보정 거리는 다음과 같이 계산하였다.

$$
d_{\mathrm{corr},i} = a_i \hat{d}_i + b_i
$$

여기서 $\hat{d}_i$는 앵커 $i$에서 측정된 거리이고, $d_{\mathrm{corr},i}$는 보정된 거리이다. $a_i$와 $b_i$는 학습 데이터에서 구한 앵커별 보정 계수이다. 보정 계수를 학습할 때는 일반적인 최소제곱 회귀만 사용하지 않고 robust loss를 적용하였다. 거리 측정값에는 이상치가 포함될 수 있기 때문에, 단순 최소제곱 방식은 일부 큰 오차값에 의해 보정식이 왜곡될 수 있다. 따라서 robust fitting을 적용하여 큰 오차값의 영향을 줄이고, 앵커별 거리 보정 계수를 더 안정적으로 학습하였다.

보정 후에는 앵커별 residual을 계산하였다. 학습 데이터에서 앵커 $i$의 보정 residual은 실제 거리와 보정 거리의 차이로 계산하였다. 보정 후에도 residual이 작은 앵커는 학습 데이터에서 안정적으로 동작한 앵커라고 보고 높은 history 신뢰도를 부여하였다. 반대로 보정 후 residual이 큰 앵커는 기본 신뢰도를 낮게 설정하였다. 즉, history 신뢰도는 각 앵커가 학습 데이터에서 평균적으로 얼마나 믿을 만했는지를 나타내는 값이다. 최종적으로 train.py는 앵커별 보정 계수 $a_i$, $b_i$, history 신뢰도, residual 통계량, Adaptive MCC에 필요한 scale 파라미터를 model.npz로 저장한다.

추론 단계에서는 DH_FR1.mat에서 앵커 좌표와 거리 측정값을 읽고, model.npz에서 학습된 보정 계수와 history 신뢰도를 불러온다. 먼저 입력 거리값을 앵커별 보정식에 넣어 $d_{\mathrm{corr},i}$로 변환한다. 이후 보정된 거리값을 이용하여 Calibrated Least Squares 기반의 기준 위치 $x_{\mathrm{base}}$를 계산한다. 이 위치는 최종 위치라기보다, 현재 입력에서 각 앵커의 residual과 영향도를 진단하기 위한 기준 위치로 사용된다.

기준 위치 $x_{\mathrm{base}}$가 계산되면, 각 앵커의 residual을 다음과 같이 계산한다.

$$
r_i = \left\|x_{\mathrm{base}} - p_{\mathrm{bs},i}\right\| - d_{\mathrm{corr},i}
$$

여기서 $p_{\mathrm{bs},i}$는 앵커 $i$의 좌표이다. $r_i$는 기준 위치에서 예측되는 거리와 보정된 측정 거리의 차이를 의미한다. 이 값이 크면 해당 앵커의 거리값이 현재 위치 추정 결과와 잘 맞지 않는다는 뜻이다. 하지만 residual만으로는 그 앵커가 실제로 최종 위치 결과를 얼마나 흔드는지까지 알기 어렵기 때문에, 본 알고리즘에서는 Leave-One-Out 자기진단을 추가로 수행하였다.

Leave-One-Out 자기진단에서는 앵커를 하나씩 제거하면서 위치를 다시 계산한다. 전체 앵커를 사용해 얻은 기준 위치를 $x_{\mathrm{base}}$라고 하고, 앵커 $i$를 제거한 상태에서 다시 계산한 위치를 $x_{-i}$라고 한다. 앵커 $i$의 영향도 $I_i$는 두 위치 사이의 거리로 계산한다.

$$
I_i = \left\|x_{\mathrm{base}} - x_{-i}\right\|
$$

$I_i$가 크다는 것은 앵커 $i$를 제거했을 때 위치 결과가 크게 바뀐다는 뜻이다. 즉, 해당 앵커가 전체 위치추정 결과에 큰 영향을 주고 있을 가능성이 있다. 반대로 $I_i$가 작다면 해당 앵커를 제거해도 위치 결과가 크게 변하지 않으므로 비교적 안정적인 앵커라고 볼 수 있다.

다만 $I_i$가 크다고 해서 해당 앵커를 무조건 나쁜 앵커로 판단하지는 않았다. 어떤 앵커는 기하학적으로 중요한 위치에 있기 때문에 제거했을 때 위치가 크게 변할 수도 있기 때문이다. 그래서 본 알고리즘에서는 LOO 영향도만 단독으로 사용하지 않고, residual 이상 정도와 함께 사용하였다. 즉, 위치를 크게 흔드는 앵커이면서 동시에 residual 관점에서도 의심스러운 앵커일수록 신뢰도를 더 낮추도록 설계하였다.

residual의 이상 정도는 MAD를 이용하여 계산하였다. 먼저 현재 입력에 대한 residual들의 중앙값을 구하고, 각 residual이 중앙값에서 얼마나 벗어났는지를 MAD 기준으로 정규화하였다.

$$
\mathrm{MAD}_r = \mathrm{median}\left(\left|r_i - \mathrm{median}(r)\right|\right)
$$

$$
z_i = \frac{\left|r_i - \mathrm{median}(r)\right|}{\mathrm{MAD}_r + \epsilon}
$$

여기서 $z_i$는 앵커 $i$의 residual이 전체 residual 분포에서 얼마나 벗어났는지를 나타낸다. MAD는 평균과 표준편차보다 이상치에 덜 민감하기 때문에, 거리 측정값에 큰 오차가 섞여 있는 상황에서도 residual 분포의 scale을 비교적 안정적으로 추정할 수 있다.

LOO 영향도 역시 전체 앵커의 영향도 분포 안에서 상대적으로 해석하였다. 따라서 $I_i$도 중앙값과 MAD를 이용해 정규화된 영향도 점수로 변환하였다.

$$
\mathrm{MAD}_I = \mathrm{median}\left(\left|I_i - \mathrm{median}(I)\right|\right)
$$

$$
\mathrm{infl}_i = \frac{\left|I_i - \mathrm{median}(I)\right|}{\mathrm{MAD}_I + \epsilon}
$$

여기서 $\mathrm{infl}_i$는 앵커 $i$의 LOO 영향도가 다른 앵커들에 비해 얼마나 큰지를 나타낸다. 이후 residual 이상 정도에 따라 LOO 영향도가 신뢰도에 반영되는 강도를 조절하였다.

$$
\mathrm{residual\_factor}_i = 0.32 + 0.68 \cdot \mathrm{clip}\left(\frac{z_i}{3.2}, 0, 1\right)
$$

$$
\mathrm{instability}_i = \mathrm{infl}_i \cdot \mathrm{residual\_factor}_i
$$

이렇게 설정한 이유는 LOO 영향도만 큰 앵커를 바로 나쁜 앵커로 판단하지 않기 위해서이다. residual 이상 정도가 작다면 해당 앵커는 단순히 기하학적으로 중요한 앵커일 수 있으므로 LOO 영향도를 약하게 반영한다. 반대로 residual 이상 정도가 크면 해당 앵커는 위치 결과를 흔드는 동시에 측정값도 의심스러운 앵커일 가능성이 높으므로 LOO 영향도를 더 강하게 반영한다. 여기서 0.32와 3.2는 validation split을 통해 선택한 값이다.

LOO 자기진단 신뢰도는 다음 형태로 계산하였다.

$$
S_{\mathrm{LOO},i} = \frac{1}{1 + 0.90 \cdot \mathrm{instability}_i}
$$

여기서 0.90은 LOO 영향도를 최종 신뢰도에 반영하는 강도이며, validation split을 통해 선택하였다. 이 값이 너무 크면 특정 앵커를 과도하게 낮게 평가할 수 있고, 너무 작으면 불안정 앵커를 충분히 억제하지 못할 수 있다.

MAD 기반 신뢰도 $S_{\mathrm{MAD},i}$는 residual이 전체 residual 분포에서 많이 벗어날수록 작아지도록 설정하였다. 즉, residual이 중앙값 근처에 있는 앵커는 높은 신뢰도를 받고, residual이 크게 벗어난 앵커는 낮은 신뢰도를 받는다. 이 방식은 특정 threshold를 넘는 앵커를 바로 제거하는 방식이 아니라, residual 이상 정도에 따라 앵커의 신뢰도를 연속적으로 조절하는 방식이다.

또한 Adaptive MCC weight를 적용하였다. MCC는 residual이 작은 데이터에는 큰 가중치를 주고, residual이 큰 데이터에는 작은 가중치를 주는 robust weight 방식이다. 본 알고리즘에서는 MCC의 scale 값을 고정된 값으로 두지 않고, 현재 residual 분포의 MAD를 이용해 자동으로 조절하였다. 이를 통해 입력마다 residual 크기가 달라져도 특정 고정 임계값에 과도하게 의존하지 않도록 하였다. MCC weight는 다음과 같은 형태로 계산된다.

$$
w_{\mathrm{MCC},i} = \exp\left(-\frac{r_i^2}{2\sigma^2}\right)
$$

여기서 $\sigma$는 현재 residual 분포의 MAD를 기반으로 정해지는 adaptive scale이다. $\sigma$가 너무 작거나 너무 커지는 것을 막기 위해 학습 단계에서 저장한 최소 scale과 최대 scale 범위를 함께 사용하였다.

최종 앵커 가중치 $W_i$는 history 신뢰도, LOO 자기진단 신뢰도, MAD 기반 신뢰도, MCC weight를 모두 곱하여 계산하였다.

$$
W_i = S_{\mathrm{history},i} \cdot S_{\mathrm{LOO},i} \cdot S_{\mathrm{MAD},i} \cdot w_{\mathrm{MCC},i}
$$

여기서 $S_{\mathrm{history},i}$는 학습 데이터에서 앵커 $i$가 얼마나 안정적이었는지를 나타내고, $S_{\mathrm{LOO},i}$는 앵커 $i$를 제거했을 때 위치 결과가 얼마나 변하는지를 반영한다. $S_{\mathrm{MAD},i}$는 현재 residual 분포에서 앵커 $i$가 이상치에 가까운지를 나타내며, $w_{\mathrm{MCC},i}$는 Adaptive MCC 기반 residual weight이다.

최종 위치는 위에서 계산한 $W_i$를 사용하여 weighted nonlinear least squares로 계산하였다. 최적화의 목표는 각 앵커에 대해 예측 거리와 보정 거리의 차이를 줄이는 것이다.

$$
\hat{x} = \arg\min_x \sum_i W_i \left(\left\|x - p_{\mathrm{bs},i}\right\| - d_{\mathrm{corr},i}\right)^2
$$

구현에서는 위 목적식과 동일하게 동작하도록 각 residual에 $\sqrt{W_i}$를 반영하여 최적화를 수행하였다. 즉, 최적화 함수 내부에서는 $\sqrt{W_i}(\left\|x - p_{\mathrm{bs},i}\right\| - d_{\mathrm{corr},i})$ 형태의 residual을 사용하였다. 이렇게 하면 최종 목적식은 $W_i$가 곱해진 제곱 오차의 합이 된다. 이 식에서 신뢰도가 높은 앵커는 최종 위치 계산에 크게 반영되고, 불안정하다고 판단된 앵커는 영향이 줄어든다.

최종 위치를 한 번 계산한 뒤에는 새 위치를 기준으로 residual, MAD 기반 신뢰도, MCC weight를 다시 계산하고 2차 weighted nonlinear least squares를 수행하였다. 첫 번째 최적화는 기준 위치 $x_{\mathrm{base}}$에서 계산한 residual을 기반으로 하므로, 기준 위치가 완벽하지 않을 경우 일부 weight가 부정확할 수 있다. 따라서 한 번 보정된 위치를 기준으로 residual과 weight를 다시 계산하면 최종 위치를 더 안정적으로 조정할 수 있다. 이때 LOO 자기진단 신뢰도는 기준 위치에서 계산한 앵커 영향도를 그대로 사용하고, MAD 기반 신뢰도와 MCC weight만 새 위치 기준으로 갱신하였다.

2차 최적화 후에는 최종 안정성 검사를 적용하였다. 이는 LOO와 robust weight가 특정 샘플에서 과도하게 작동하여 오히려 위치를 악화시키는 경우를 줄이기 위한 장치이다. 먼저 기준 위치 $x_{\mathrm{base}}$와 2차 최적화 위치 $x_{\mathrm{loo2}}$의 weighted residual score를 비교하였다. 2차 최적화 위치의 score가 기준 위치보다 작거나 같으면 $x_{\mathrm{loo2}}$를 최종 위치로 사용하였다. 만약 2차 최적화 위치의 score가 기준 위치보다 약간만 크고, 두 위치 사이의 이동량이 과도하지 않다면 기준 위치와 2차 최적화 위치를 혼합하여 사용하였다. 이때 2차 최적화 결과가 어느 정도 신뢰 가능하다고 판단되는 경우에는 $0.75x_{\mathrm{loo2}} + 0.25x_{\mathrm{base}}$ 형태로 사용하고, 개선 정도가 작지만 이동량이 매우 작은 경우에는 $0.5x_{\mathrm{loo2}} + 0.5x_{\mathrm{base}}$ 형태로 사용하였다. 반대로 2차 최적화 위치의 score가 기준 위치보다 충분히 좋지 않거나 이동량이 과도하게 크면 $x_{\mathrm{base}}$를 유지하였다. 이를 통해 robust weight가 일부 샘플에서 지나치게 강하게 작동하여 위치가 튀는 문제를 줄이고자 하였다.

main.py의 main 함수는 최종적으로 numpy 배열 형태의 $p_{\mathrm{hat}}$을 반환한다. 반환 배열의 모양은 $(2, num\_user)$이며, 첫 번째 행은 x 좌표, 두 번째 행은 y 좌표이다. hidden test에서는 사용자 수가 공개 데이터와 다를 수 있으므로, 사용자 수를 코드에 직접 고정하지 않고 $num\_user = d\_hat.shape[1]$을 이용해 자동으로 계산하도록 구현하였다.


# Agent AI(e.g., ChatGPT, Claude Code, Gemini 등) 활용 방안

본 프로젝트에서는 ChatGPT를 알고리즘 아이디어 비교, 구현 방향 점검, 오류 원인 분석, 결과 해석, 보고서 문장 정리에 활용하였다. 다만 ChatGPT가 제안한 내용을 그대로 제출한 것은 아니며, 본인이 직접 데이터 구조를 확인하고 코드를 실행하면서 성능을 비교한 뒤 최종 알고리즘을 선택하였다. 즉, ChatGPT는 의사결정을 대신한 도구가 아니라, 알고리즘 설계와 구현 과정에서 선택지를 정리하고 검토를 돕는 보조 도구로 사용하였다.

알고리즘 설계 단계에서는 중간발표까지 사용했던 앵커별 거리 보정, DBSCAN 기반 후보 군집화, MAD 기반 이상치 판단, MCC 기반 robust weighting의 역할을 다시 검토하였다. 본인은 중간발표 피드백을 바탕으로 각 기법이 현재 데이터의 어떤 문제를 해결하는지 다시 정리하였다. 이 과정에서 ChatGPT는 각 방법의 장점과 한계를 비교하는 데 도움을 주었고, 본인은 hidden test에서의 안정성, 구현 가능성, 성능 개선 가능성을 기준으로 최종 방향을 결정하였다. 그 결과 기존에 고려했던 앵커별 보정, MAD, MCC는 유지하되, DBSCAN 후보 군집화 대신 Leave-One-Out 자기진단 방식을 핵심 아이디어로 선택하였다.

구현 단계에서는 ChatGPT를 이용해 train.py와 main.py의 역할 분리를 점검하였다. 본인은 제공된 DH_FR1.mat 데이터의 구조를 확인하고, 학습 단계에서 사용할 수 있는 정답 위치와 hidden test 단계에서 사용할 수 없는 정보를 구분하였다. 이를 바탕으로 train.py에서는 정답 위치를 이용해 앵커별 보정 계수와 history 신뢰도를 학습하고, main.py에서는 정답 위치 없이 model.npz와 입력 거리값만 사용하여 위치를 추정하도록 구현하였다. ChatGPT는 코드 구조와 오류 원인 분석을 보조하였고, 본인은 실제 터미널에서 코드를 실행하여 main 함수의 반환 형태, 사용자 수 자동 처리, model.npz 로딩 여부를 확인하였다.

성능 검증 단계에서는 본인이 직접 Raw Least Squares, Calibrated Least Squares, LOO-TWAMCC를 실행하여 결과를 비교하였다. ChatGPT는 결과를 표로 정리하고, 어떤 baseline과 비교하는 것이 공정한지 논리적으로 정리하는 데 도움을 주었다. 본인은 Raw Least Squares와의 비교만으로는 제안 알고리즘의 실제 기여를 판단하기 어렵다고 보았고, 앵커별 보정을 동일하게 적용한 Calibrated Least Squares를 더 공정한 비교 대상으로 설정하였다.

파라미터 선택 단계에서도 ChatGPT를 보조적으로 활용하였다. 본인은 전체 700개 공개 데이터에만 맞춰 파라미터를 고르면 hidden test에 과적합될 수 있다고 판단하였다. 이에 따라 validation split 기반으로 일부 데이터를 분리하고, 나머지 데이터로 다시 학습한 뒤 validation data에서 성능을 확인하는 방식으로 tune.py를 구성하였다. 여러 random seed를 사용하여 특정 split에만 우연히 잘 맞는 파라미터가 선택되지 않도록 하였고, 최종 파라미터는 이러한 검증 결과를 바탕으로 본인이 선택하였다.

보고서 작성 단계에서는 ChatGPT를 문장 정리와 형식 점검에 활용하였다. 본인은 실제 구현 과정, 실험 결과, 알고리즘 수정 이유, baseline 비교 방식, validation split 결과를 제공하였고, ChatGPT는 이를 report.md의 필수 섹션 형식에 맞게 정리하는 데 도움을 주었다. 최종 보고서 내용은 본인이 직접 검토하여 실제 구현과 다르지 않은지 확인하였다.

정리하면, ChatGPT는 알고리즘 후보 비교, 코드 구조 점검, 오류 원인 분석, 결과 해석 보조, 보고서 표현 정리에 사용되었다. 반면 본인은 데이터 특성 파악, 최종 알고리즘 선택, 코드 실행과 성능 검증, baseline 비교의 공정성 판단, hidden test를 고려한 파라미터 선택을 직접 수행하였다. 따라서 본 프로젝트에서 AI는 최종 판단을 대신한 것이 아니라, 본인이 설계와 검증을 수행하는 과정에서 보조적으로 활용한 Agent AI이다.

# 결과 도출 & 디스커션

최종 알고리즘의 성능을 확인하기 위해 Raw Least Squares, Calibrated Least Squares, LOO-TWAMCC를 비교하였다. Raw Least Squares는 거리 보정 없이 측정 거리값을 그대로 사용하는 기본 방식이고, Calibrated Least Squares는 train.py에서 학습한 앵커별 거리 보정만 적용한 방식이다. LOO-TWAMCC는 Calibrated Least Squares에 Leave-One-Out 자기진단, MAD 기반 residual score, Adaptive MCC weight를 추가한 최종 알고리즘이다.

| Method                   |     MAE |    RMSE |  Median |     P90 |     Max |
| ------------------------ | ------: | ------: | ------: | ------: | ------: |
| Raw Least Squares        | 23.1074 | 24.8694 | 21.6895 | 33.4384 | 80.7036 |
| Calibrated Least Squares | 11.3935 | 14.1249 |  9.4372 | 21.2485 | 69.4275 |
| LOO-TWAMCC               |  9.7898 | 12.2692 |  7.8155 | 18.4957 | 56.6512 |

Raw Least Squares는 전체적으로 오차가 크게 나타났다. 이는 측정 거리값에 포함된 앵커별 bias와 이상치가 그대로 위치추정에 반영되었기 때문으로 볼 수 있다. Calibrated Least Squares는 Raw Least Squares보다 성능이 크게 개선되었다. 이를 통해 앵커별 거리 보정이 위치추정 성능 향상에 중요한 역할을 한다는 것을 확인할 수 있었다. 하지만 본 알고리즘의 실제 기여를 판단하기 위해서는 Raw Least Squares보다 Calibrated Least Squares와 LOO-TWAMCC를 비교하는 것이 더 적합하다고 판단하였는데, LOO-TWAMCC도 동일한 앵커별 보정 계수를 사용하기 때문에 단순히 거리 보정이 만든 성능 향상을 제안 알고리즘의 기여로 보기는 어려웠다. 따라서 본 알고리즘의 핵심 기여는 Calibrated Least Squares 이후에 추가된 Leave-One-Out 자기진단, MAD score, Adaptive MCC weight가 얼마나 성능을 향상시켰는지로 판단하는 것이 타당하다고 보았다. 이러한 기준에서 Calibrated Least Squares와 비교했을 때, LOO-TWAMCC는 모든 지표에서 성능이 개선되었다.

| Comparison    |     MAE |    RMSE | Median |     P90 |     Max |
| ------------- | ------: | ------: | -----: | ------: | ------: |
| Calibrated LS | 11.3935 | 14.1249 | 9.4372 | 21.2485 | 69.4275 |
| LOO-TWAMCC    |  9.7898 | 12.2692 | 7.8155 | 18.4957 | 56.6512 |
| Improvement   |   14.1% |   13.1% |  17.2% |   13.0% |   18.4% |

위 결과에서 MAE, RMSE, Median error가 모두 감소한 것은 전체적인 평균 성능뿐만 아니라 일반적인 샘플에 대한 위치추정 성능도 개선되었음을 의미한다. 또한 P90 error와 Max error가 함께 감소한 것은 일부 불안정 앵커로 인해 발생하는 큰 오차를 줄이는 데 LOO 자기진단 가중치가 효과적이었다는 것을 보여준다.

파라미터 선택 과정에서도 hidden test에 대한 일반화 가능성을 고려하였다. 전체 700개 데이터에서 가장 좋은 값만 선택하면 공개 데이터에 과적합될 수 있다. 따라서 별도의 tune.py를 작성하여 validation split 기반 검증을 수행하였다. 전체 데이터 일부를 validation set처럼 분리하고, 나머지 데이터로 보정 계수와 history 신뢰도를 다시 학습한 뒤 validation data에서 성능을 확인하였다. 이 과정을 여러 random seed에서 반복하여 특정 split에만 우연히 잘 맞는 파라미터를 피하고자 하였다. 이때 검증한 주요 파라미터는 residual_base, residual_div, loo_gain이었다. 이들은 LOO 영향도와 residual 이상 정도를 최종 신뢰도에 얼마나 강하게 반영할지를 결정한다. validation split 결과를 바탕으로 최종 설정은 다음과 같이 선택하였다.

| Parameter     | Selected value |
| ------------- | -------------: |
| residual_base |           0.32 |
| residual_div  |            3.2 |
| loo_gain      |           0.90 |

이 자체 평가 방식은 hidden test 상황을 완전히 재현할 수는 없지만, 전체 700개 데이터를 한 번에 모두 사용해 파라미터를 고르는 것보다는 더 공정하다고 판단하였다. validation으로 분리한 데이터는 파라미터 학습에 사용하지 않았기 때문에, 알고리즘이 보지 않은 데이터에서 어느 정도 안정적으로 동작하는지 확인할 수 있었다. 또한 하나의 split에만 의존하지 않고 여러 random seed를 사용하여 결과가 특정 데이터 분할에만 우연히 맞는 문제를 줄이고자 하였다. 이 설정은 LOO 영향도를 너무 강하게 적용하지 않고, 불안정 앵커의 영향을 완만하게 줄이는 방향이다. 실제 validation 결과에서도 LOO 영향도를 과도하게 반영하는 것보다 완만하게 반영하는 설정이 더 안정적으로 나타났다. 따라서 최종 main.py에는 이 파라미터를 적용하였다.

본인의 구현 방향은 문제 상황에 적합하다고 판단하였다. 단순히 복잡한 모델을 추가하는 것보다, 실제 데이터에서 확인된 문제인 앵커별 bias와 불안정 앵커의 영향을 줄이는 데 집중했기 때문이다. train.py에서는 앵커별 거리 보정을 통해 고정적인 bias를 줄였고, main.py에서는 Leave-One-Out 자기진단과 MAD, MCC weight를 사용하여 입력마다 달라지는 불안정 앵커의 영향을 줄이도록 하였다. 따라서 학습 단계와 추론 단계의 역할이 명확하게 분리되어 있으며, hidden test에서 사용할 수 없는 정답 위치를 main.py에서 사용하지 않도록 구현한 점도 적합하다고 판단하였다.

본 알고리즘은 단순히 residual이 큰 앵커를 낮게 평가하는 방식에서 한 단계 더 나아가, 앵커가 전체 위치추정 구조에 미치는 영향을 직접 반영한다는 장점이 있다. 일반적인 residual 기반 방식은 현재 위치에서 각 앵커의 거리 오차가 큰지만 확인하지만, LOO-TWAMCC는 특정 앵커를 제거했을 때 위치 결과가 얼마나 변하는지를 함께 확인한다. 따라서 어떤 앵커가 단순히 오차가 큰 앵커인지, 아니면 실제로 전체 위치 결과를 불안정하게 만드는 앵커인지 더 구체적으로 판단할 수 있다.

또한 train.py에서 앵커별 거리 보정 계수와 history 신뢰도를 학습하기 때문에, 모든 앵커를 동일하게 취급하지 않고 앵커별 오차 특성을 반영할 수 있다. 실제 거리 측정 환경에서는 앵커마다 설치 위치, 주변 구조물, 신호 반사 환경이 다르기 때문에 오차 특성도 다르게 나타날 수 있는데, 본 알고리즘은 이러한 차이를 학습 단계에서 보정하고, 추론 단계에서는 LOO 자기진단과 residual 기반 가중치를 사용하여 입력마다 달라지는 불안정 앵커의 영향을 줄이도록 하였다.

마지막으로 MAD와 Adaptive MCC를 함께 사용하여 이상치에 대해 비교적 부드럽게 대응할 수 있었다. 단순 threshold 방식처럼 특정 기준을 넘는 앵커를 바로 제거하는 것이 아니라, residual 분포를 기준으로 앵커의 신뢰도를 연속적으로 조절하였다. 이 방식은 일부 앵커의 측정값이 불안정하더라도 전체 위치추정 과정이 급격하게 흔들리지 않도록 하는 데 도움이 되었다.

반면 한계도 있다. Leave-One-Out 방식은 각 사용자마다 앵커를 하나씩 제거하면서 위치를 다시 계산하므로 단순 Least Squares보다 계산량이 많다. 현재 데이터에서는 앵커 수가 18개로 제한되어 있어 실행 시간 제한 안에서 동작할 수 있었지만, 앵커 수가 훨씬 많아지는 환경에서는 계산량 최적화가 필요할 수 있다. 또한 기준 위치 \(x_{\text{base}}\)가 크게 잘못 계산되는 경우, LOO 영향도 평가도 영향을 받을 수 있다.

향후 개선 방향으로는 LOO 계산을 더 효율적으로 근사하는 방법을 고려할 수 있다. 예를 들어 모든 앵커를 제거해 다시 최적화하는 대신, 선형화된 영향도 근사식을 사용하면 계산량을 줄일 수 있다. 또한 앵커의 기하학적 배치까지 함께 고려하면, 중요한 앵커와 불안정한 앵커를 더 정교하게 구분할 수 있을 것이다.

결론적으로 LOO-TWAMCC는 단순 거리 보정 방식보다 평균 오차와 큰 오차를 모두 줄였다. 또한 baseline과의 비교도 Calibrated Least Squares를 기준으로 수행하여 비교의 공정성을 확보하고자 하였다. 따라서 본 알고리즘은 주어진 데이터셋에서 성능, 안정성, 해석 가능성 측면에서 적합한 최종 위치추정 알고리즘이라고 판단하였다.